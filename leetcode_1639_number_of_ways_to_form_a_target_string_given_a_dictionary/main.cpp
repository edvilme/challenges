#include <vector>
#include <iostream>
using namespace std;

class Solution {
    public:
    int numWays(vector<string>& words, string target) {
        int count = 0;
        int wordSize = words[0].size();
        
    }
}